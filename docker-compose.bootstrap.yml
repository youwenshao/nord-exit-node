# Temporary stack: authenticate Tailscale BEFORE attaching to Nord.
# Usage: docker compose -f docker-compose.bootstrap.yml up -d
# After login + exit-node approval, switch to docker-compose.yml
name: nord-exit
services:
  tailscale:
    image: tailscale/tailscale:latest
    container_name: nord-exit-tailscale
    hostname: nord-exit
    env_file:
      - .env.tailscale
    environment:
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
      - TS_HOSTNAME=${TS_HOSTNAME:-nord-exit}
    # Run tailscaled directly so interactive login is not killed by
    # containerboot's ~60s auth deadline.
    entrypoint: ["tailscaled"]
    command: ["--statedir=/var/lib/tailscale", "--tun=tailscale0"]
    volumes:
      - ./state:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
      - NET_RAW
    restart: "no"
